class Weather {
  String location, updatedTime, image, temp, maxTemp, minTemp, weather;

  Weather({
    required this.location,
    required this.updatedTime,
    required this.image,
    required this.temp,
    required this.maxTemp,
    required this.minTemp,
    required this.weather,
  });
}
